# تقرير تشخيص أسباب عدم ظهور الإعلانات بعد الدمج في النطاق الرئيسي (zoonasd.com)

## الخلاصة والملخص التنفيذي
تم إجراء تشخيص دقيق وشامل لأسباب ظهور الإعلانات في بيئة الاختبار (الفرع/Nomic/Vercel preview) وعدم ظهورها بعد الدمج في الفرع الرئيسي (`main`) والنطاق الرئيسي (`zoonasd.com`).

السبب الرئيسي **ليس خطأً برمجياً في كود الإعلانات نفسه** (حيث إن وسوم الإعلانات `<ins>` وسكربت `data527.click` مضافة بشكل صحيح في أجزاء الصفحة)، وإنما يعود إلى **قيود واشتراطات شبكة الإعلانات (Ad Network Requirements)** وسياسات النطاقات المعتمدة (Domain Approval & Referrer Policies).

---

## الأسباب التقنية التفصيلية

### 1. عدم اعتماد النطاق الرئيسي (zoonasd.com) في لوحة شبكة الإعلانات (AdMaven / data527.click)
* **كيف تعمل شبكات الإعلانات:** عند قيام المتصفح بطلب الإعلان من السكربت `//data527.click/js/responsive.js`، يفحص خادم الإعلانات رأس الطلب (`HTTP Referer` / `Origin`) لمعرفة النطاق الذي يطلب الإعلان.
* **سبب ظهوره في الفرع قبل الدمج:** في بيئة الاختبار (مثل روابط المعاينة `*.vercel.app` أو النطاقات الفرعية)، تكون الإعلانات شغالة إما لأن نطاق Vercel يمرر في طور الاختبار (Test Mode/Sandbox)، أو أن النطاق المخصص للاختبار كان مقبولاً تلقائياً.
* **سبب التوقف في main:** عند الانتقال إلى `zoonasd.com`، ترفض خوادم الإعلانات الاستجابة (أو ترجع استجابة فارغة `Fill Rate = 0%`) إذا لم يكن النطاق الرئيسي `zoonasd.com` قد تم **إضافته وتأكيده وتفعيله رسمياً (Approved Domain)** داخل لوحة تحكم الناشر في AdMaven / data527.click.

### 2. قيود سياسة المرجعية (Referrer Header Policy)
* شبكات الإعلانات تعتمد على معرفة النطاق الحالي للتحقق من الشرعية ومحاربة الاحتيال (Anti-Fraud).
* إذا لم تحتوي الصفحة على وسم المرجعية `<meta name="referrer" content="no-referrer-when-downgrade">`، فقد تقوم المتصفحات الحديثة (مثل Chrome, Safari) بإخفاء أو تقليل بيانات الـ Referer بين النطاقات المختلفة (Cross-Origin)، مما يمنع شبكة الإعلانات من التعرف على `zoonasd.com`.

### 3. عدم تطابق رمز التحقق أو الموضع الإعلاني (Placement ID / Meta Tag)
* يحتوي `index.html` على الوسم:
  ```html
  <meta name='admaven-placement' content="BpdU9qjU7">
  ```
* إذا كان هذا الرمز `BpdU9qjU7` مرادطاً بمساحة تجريبية أو نطاق سابق، فلن تظهر الإعلانات على `zoonasd.com` إلا بعد تحديث الرمز للرمز الخاص بالنطاق المعتمد الجديد، أو استكمال خطوة تأكيد ملكية النطاق (Domain Verification Challenge).

### 4. حجب الإعلانات أو تقنيات التخزين المؤقت (Caching & Service Worker)
* المتصفحات وحاجبات الإعلانات (AdBlock / Safari Tracking Protection) تحجب نطاق `data527.click` إذا صنفته كشبكة إعلانات غير معتمدة للنطاق.

---

## خطوات الحل والعمل المطلوب للتشغيل الفوري

### الخطوة 1: إجراءات لوحة تحكم AdMaven / data527 (إلزامية)
1. تسجيل الدخول إلى لوحة الناشر في **AdMaven / data527.click**.
2. الذهاب إلى قسم **Domains / Websites**.
3. إضافة النطاق الرئيسي: `https://zoonasd.com`.
4. استكمال خطوة **Domain Verification** (إما بإضافة Meta Tag مخصص أو رفع ملف اثبات الملكية في المجلد الرئيسي).
5. التأكد من أن حالة النطاق أصبحت **Approved / Active**.
6. نسخ رمز المساحة الإعلانية (Placement Tag / Placement ID) الجديد الخاص بالنطاق `zoonasd.com` وتحديثه في الكود إذا لزم الأمر.

### الخطوة 2: التحديثات البرمجية المنفذة في الكود
1. التأكد من وجود وسم المرجعية `<meta name="referrer" content="no-referrer-when-downgrade">` في `index.html` وجميع صفحات المتجر لضمان وصول هيدر النطاق كاملاً لشبكة الإعلانات.
2. الحفاظ على هيكلية إعلانات responsive داخل `<div id="frame">`.
